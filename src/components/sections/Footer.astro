---
import { imgsFooter } from "@data/dataFooter";
import { Image } from "astro:assets";
---

<footer class="h-screen">
  <section
    class="w-full h-1/2 overflow-hidden flex items-center justify-center"
  >
    <section
      class="w-full h-full relative flex justify-center items-center overflow-hidden px-[10vw]"
    >
      <div
        class="absolute top-0 left-0 right-0 bottom-0 m-auto w-4/5 h-1/2 bg-white/80 flex flex-col justify-center items-center gap-2 laptop:gap-4 laptop:w-1/2"
      >
        <p class="text-gray-600">FACEBOOK SE SIENTE</p>
        <p class="text-gray-600 prose-sm">síguenos en @FOTO Y VIDEO GABY</p>
      </div>
      {
        imgsFooter !== null ? (
          imgsFooter.map((img, index) => (
            <div
              class="flex-shrink-0 w-1/2 h-full  overflow-hidden laptop:w-1/3"
              class:list={{
                "hidden laptop:block": !img.mobileOnly,
              }}
            >
              <Image
                src={img.urlImage}
                alt={`Imagen footer #${index + 1}`}
                class="w-full h-full object-cover object-top hover:cursor-pointer"
              />
            </div>
          ))
        ) : (
          <p class="text-center w-full">No hay imágenes disponibles</p>
        )
      }
    </section>
  </section>
  <section
    class="w-full flex flex-col items-center justify-center phone-lg:min-h-1/2"
  >
    <div class="flex-1 w-full flex flex-col tablet:flex-row">
      <aside class="w-full relative flex justify-center items-center">
        <div
          class="w-3/4 h-48 relative overflow-hidden my-4 text-gray-muted-50"
        >
          <!-- Letras -->
          <h3 class="absolute top-4 left-1/2 -translate-x-1/2">F</h3>
          <h3
            class="absolute left-8 top-1/2 -translate-y-1/2 text-2xl font-semibold"
          >
            F
          </h3>
          <h3
            class="absolute bottom-4 left-1/2 -translate-x-1/2 text-2xl font-semibold"
          >
            V
          </h3>
          <h3
            class="absolute right-8 top-1/2 -translate-y-1/2 text-2xl font-semibold"
          >
            G
          </h3>

          <!-- Línea 1: de top-left a bottom-right -->
          <div
            class="absolute left-1/2 top-1/2 w-[150%] h-[1px] bg-gray-muted-50 rotate-45 -translate-x-1/2 -translate-y-1/2"
          >
          </div>

          <!-- Línea 2: de top-right a bottom-left -->
          <div
            class="absolute left-1/2 top-1/2 w-[150%] h-[1px] bg-gray-muted-50 -rotate-45 -translate-x-1/2 -translate-y-1/2"
          >
          </div>
        </div>
      </aside>
      <aside
        class="w-full flex flex-col justify-center items-center text-center px-4"
      >
        <h3 class="text-3xl mb-6 text-gray-700">FOTO Y VIDEO GABY</h3>
        <p class="prose-sm w-3/4 mb-3 text-gray-muted-50">
          Sesiones fotográficas, Cobertura de eventos, Producción de video,
          Impresión de fotos, Fotografía para documentos, Entrega en formatos
          digitales y físicos
        </p>
        <p class="text-gray-muted-50">
          Copyright © 2025 Foto y Video Gaby. Todos los derechos reservados.
        </p>
      </aside>
    </div>
    <div class="bg-gray-100 p-4 w-full text-center">
      <h3 class="text-gray-muted">Desarrollado por: Alexis🧢Chim</h3>
    </div>
  </section>
</footer>
